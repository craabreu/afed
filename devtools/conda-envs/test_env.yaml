name: test
channels:
  - omnia
dependencies:
    # Base depends
  - python
  - pip

  - openmm >=7.3.1

    # Testing
  - isort
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
